#include <Audio/cola_reproduccion.h>
#include <Audio/player.h>

void reproducirPlaylist(Playlist *playlist)
{
    /* Lógica para la cola de reproducción */

    // Cancion *cancion = NULL; // Canción actual

    // while (cancion) {
    //     VibeCast_PlayAudio(cancion->archivo);
    // }
} // VibeCast - Vibes Vibras Ondas Sonido - Cast Broadcast Transmisión